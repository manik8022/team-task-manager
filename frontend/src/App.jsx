<div className="topbar">

  <div></div>

  <h1>Team Task Manager</h1>

  <button
    onClick={() =>
      setDarkMode(!darkMode)
    }
  >
    {darkMode
      ? "Light Mode"
      : "Dark Mode"}
  </button>

</div>